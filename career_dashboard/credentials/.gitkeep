# Place your Google credentials files here:
# - service_account.json  (for Service Account auth)
# - oauth_credentials.json  (for OAuth2 auth)
#
# DO NOT commit these files to version control!
# Add credentials/ to .gitignore
